program HelloWorld;

var a: Integer;
var b: Boolean;

function add(c: Integer, d: Integer): Integer;
begin
   add := c + d;
end;

procedure print(message: Integer);
begin
   writeln(c);
end;

begin
   if e > 5 then
      begin
         m := (add(n, c) > 5) = (4 / z - - x);
         add (add(s, l));
         f := true;
      end
   else
      print();
end.